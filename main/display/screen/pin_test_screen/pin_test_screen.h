#pragma once

#include "lvgl.h"
#include "screen_util.h"

// ---------------------------------------------------------------------------
// PinTestScreen
//
// 引脚测试 App：用于硬件 bring-up 时手动操作 ESP32-P4 直连 GPIO。
//   - 列表式显示一组待测的 ESP32-P4 GPIO（5/14/15/16/17/18/19/20/21/23/35/
//     46/47/48），不包含任何 IO 扩展芯片的引脚
//   - 每个引脚可独立切换 “输入 / 输出”
//       · 输出 ：用 Switch 控制电平 高 / 低
//       · 输入 ：屏幕定时回读引脚电平并实时展示
//   - 底部 “全部输出·周期” 按钮：把所有引脚设成输出并以 1s 周期翻转高 /
//     低电平（即 1s HIGH，1s LOW），用于做总线 / 排线连通性测试。
//
// 生命周期：
//   - 离开屏幕时停掉所有 lv_timer（输入轮询 + 周期方波）
//   - 不还原引脚方向 / 电平 —— 这是测试页，用户离开时通常会重启或自行恢复
// ---------------------------------------------------------------------------
class PinTestScreen {
public:
    static lv_obj_t* Create();
    static void LifecycleCallback(screen_lifecycle_event_t event);
};
