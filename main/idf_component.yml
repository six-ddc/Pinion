## IDF Component Manager Manifest File
## main 只剩 pi UI；硬件依赖全部由 components/metalio_hal 自己声明。
dependencies:
  # pi-c: local path dependency（兄弟仓库布局：Code/esp32/MetalioClaw6 ↔
  # Code/six-ddc/pi-c，勿破坏相对路径）。
  pi-c:
    path: ../../../six-ddc/pi-c
  # pi_screen 直接引用 LVGL 与 font_puhui_20_4/30_4
  lvgl/lvgl: ~9.3.0
  78/xiaozhi-fonts: ~1.5.5
  idf: '>=5.5.2'
