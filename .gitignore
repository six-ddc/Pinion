# ESP-IDF build outputs / generated state
build/
build*/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old

# project-local ESP-IDF install + activation script (machine-specific paths)
.esp-idf
.idf-env.sh

# editors / tooling
.vscode/
.devcontainer/
.clangd
.cache
.DS_Store
*.pyc
*.bin
tmp/
.env

# 误提交防护：固件已不打包密钥（都存 NVS，经 Web 后台配置），这两个文件名不再由任何
# 流程产生；仍然忽略，是为了让本地遗留的旧真钥文件永远不可能被 git add 进来。
main/display/screen/pi_screen/pi_models_data.h
volc_keys.h

# sim 运行产物（从仓库根目录运行 ./sim/build/pi_sim 时生成于 CWD）
/pi_sim_settings.ini
/pi_sim_sd/

# 串口采集日志（tools/serial_cap.py 的 OUT= 产物）与 python 缓存
serial*.log
__pycache__/
